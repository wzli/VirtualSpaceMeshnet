namespace vsm;

table NodeInfo {
  name:string;
  address:string (key);
  coordinates:[float];
  power_radius:float;
  sequence:uint32;
}

table Message {
  timestamp:uint32;
  range:float;
  expiry:uint32;
  priority:uint8;
  source:NodeInfo;
  peers:[NodeInfo];
  entities:[Entity];
}

enum SyncMode:uint8 {
  AUTHORITY,
  LATEST,
  NEAREST,
  // AVERAGE,
  CUSTOM,
}

table Entity {
  sync_mode:SyncMode;
  expiry:uint32;
  name:string (key);
  type:uint32;
  data:[uint8];
}
